<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="style.css">
    <title>Edit Student</title>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Edit Student</h1>
        <form action="/update/<%= student._id %>" method="POST">
            <div class="form-group">
                <label for="first_name">First Name:</label>
                <input type="text" id="first_name" name="first_name" class="form-control" value="<%= student.first_name %>">
            </div>
            <div class="form-group">
                <label for="last_name">Last Name:</label>
                <input type="text" id="last_name" name="last_name" class="form-control" value="<%= student.last_name %>">
            </div>
            <div class="form-group">
                <label for="email">Email ID:</label>
                <input type="email" id="email" name="email" class="form-control" value="<%= student.email %>">
            </div>
            <div class="form-group">
                <label>Gender:</label><br>
                <input type="radio" id="female" name="gender" value="female" <%= student.gender === 'female' ? 'checked' : '' %>> Female
                <input type="radio" id="male" name="gender" value="male" <%= student.gender === 'male' ? 'checked' : '' %>> Male
                <input type="radio" id="other" name="gender" value="other" <%= student.gender === 'other' ? 'checked' : '' %>> Other
            </div>
            <div class="form-group">
                <label for="department">Department:</label>
                <select id="department" name="department" class="form-control">
                    <option value="cse" <%= student.department === 'cse' ? 'selected' : '' %>>CSE</option>
                    <option value="csecs" <%= student.department === 'csecs' ? 'selected' : '' %>>CSE (Cybersecurity)</option>
                    <option value="civil" <%= student.department === 'civil' ? 'selected' : '' %>>Civil</option>
                    <option value="mech" <%= student.department === 'mech' ? 'selected' : '' %>>Mechanical</option>
                </select>
            </div>
            <button type="submit" class="btn btn-success">Update</button>
            <a href="/display" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>
